import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const AudioAnimation = () => {
  return (
    <View>
      <Text>AudioAnimation</Text>
    </View>
  );
};

export default AudioAnimation;

const styles = StyleSheet.create({});
